import {useEffect,useRef,useState,type FormEvent} from 'react';
import {db,rpc} from './client';import type {Database} from './database.types';
type T=Database['public']['Tables'];type Need=T['beneficiary_needs']['Row'];type Entry=T['assistance_entries']['Row'];type Link=T['need_assistance_links']['Row'];
const val=(f:FormData,k:string)=>String(f.get(k)||'').trim();const title=(s:string)=>s.replaceAll('_',' ');
const states=['open','in_progress','met','closed','needs_review'];const categories=['food','education','health','housing','livelihood','other'];const pending=['open','in_progress','needs_review'];
function Pager({page,more,busy,onChange}:{page:number;more:boolean;busy:boolean;onChange:(n:number)=>void}){return <div className="actions"><button className="secondary" disabled={busy||page===0} onClick={()=>onChange(page-1)}>Previous</button><span>Page {page+1}</span><button className="secondary" disabled={busy||!more} onClick={()=>onChange(page+1)}>Next 50</button></div>}
export function NeedsPanel({projectId,personId,onChanged,refreshKey=0}:{projectId:string;personId?:string;onChanged:()=>void;refreshKey?:number}){
 const [rows,setRows]=useState<Need[]>([]),[names,setNames]=useState<Record<string,string>>({}),[summary,setSummary]=useState<Record<string,number|string>>({}),[today,setToday]=useState(''),[responses,setResponses]=useState<T['survey_responses']['Row'][]>([]),[status,setStatus]=useState(''),[category,setCategory]=useState(''),[overdue,setOverdue]=useState(false),[page,setPage]=useState(0),[more,setMore]=useState(false),[busy,setBusy]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState(''),[rev,setRev]=useState(0),[chosen,setChosen]=useState<string|null>(null),[request,setRequest]=useState(()=>crypto.randomUUID());
 useEffect(()=>{let live=true;setBusy(true);setError('');async function load(){const s=await rpc('project_needs_summary',{p_project:projectId,p_person:personId||null}) as Record<string,number|string>;let q=db!.from('beneficiary_needs').select('*').eq('project_id',projectId).order('follow_up_on',{ascending:true,nullsFirst:false}).order('id').range(page*50,page*50+50);if(personId)q=q.eq('person_id',personId);if(status)q=q.eq('status',status);if(category)q=q.eq('category',category);if(overdue)q=q.in('status',pending).lt('follow_up_on',String(s.utc_today));const r=await q;if(r.error)throw r.error;const items=(r.data||[]).slice(0,50);const ids=[...new Set(items.map(n=>n.person_id))];const n=ids.length?await db!.from('registry_persons').select('id,full_name,registry_no').in('id',ids):{data:[],error:null};if(n.error)throw n.error;if(live){setRows(items);setMore((r.data||[]).length>50);setSummary(s);setToday(String(s.utc_today));setNames(Object.fromEntries((n.data||[]).map(p=>[p.id,`${p.full_name} · BEN-${p.registry_no}`])))}}load().catch(e=>{if(live)setError(e.message)}).finally(()=>{if(live)setBusy(false)});return()=>{live=false}},[projectId,personId,status,category,overdue,page,rev,refreshKey]);
 useEffect(()=>{if(!personId)return;let live=true;db!.from('survey_responses').select('*').eq('person_id',personId).eq('status','approved').order('reviewed_at',{ascending:false}).limit(50).then(r=>{if(!live)return;if(r.error)setError(r.error.message);else setResponses(r.data||[])});return()=>{live=false}},[personId,rev,refreshKey]);
 function refresh(){setRev(n=>n+1);onChanged()}
 async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),form=e.currentTarget;setBusy(true);setError('');try{await rpc('create_beneficiary_need',{p_id:request,p_response:val(f,'response'),p_category:val(f,'category'),p_description:val(f,'description'),p_priority:val(f,'priority'),p_follow_up:val(f,'followup')||null,p_reason:val(f,'reason')});form.reset();setRequest(crypto.randomUUID());setMessage('Need recorded. Review existing needs before adding another.');setPage(0);refresh()}catch(e){setError((e as Error).message);setBusy(false)}}
 return <section className="needs-panel" aria-label={personId?'Person needs':'Project needs overview'}><h3>{personId?'Needs and follow-up':'Project needs overview'}</h3><p>Recorded needs: {summary.total??'—'} · People: {summary.people??'—'} · Open: {summary.open??'—'} · In progress: {summary.in_progress??'—'} · Met: {summary.met??'—'} · Needs review: {summary.needs_review??'—'} · Closed: {summary.closed??'—'}</p><p><strong>Overdue follow-ups: {summary.overdue??'—'} · High-priority pending: {summary.high_priority_pending??'—'}</strong></p><p>Counts cover all recorded needs in this {personId?'person’s project record':'project'}, independent of list filters. They do not measure all community needs or deduplicated people across projects.</p>{error&&<p className="notice error" role="alert">{error}</p>}{message&&<p role="status">{message}</p>}<div className="form-grid"><label className="field">Status<select value={status} onChange={e=>{setStatus(e.target.value);setPage(0)}}><option value="">All statuses</option>{states.map(s=><option key={s} value={s}>{title(s)}</option>)}</select></label><label className="field">Category<select value={category} onChange={e=>{setCategory(e.target.value);setPage(0)}}><option value="">All categories</option>{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><label className="checklabel"><input type="checkbox" checked={overdue} onChange={e=>{setOverdue(e.target.checked);setPage(0)}}/>Overdue pending follow-ups only (UTC)</label>{busy&&<p role="status">Loading needs…</p>}{!busy&&!rows.length&&<p>No needs match these filters.</p>}{rows.map(n=><article className="document-row" key={n.id}><strong>{n.description}</strong><p>{names[n.person_id]||n.person_id}</p><p>{n.category} · {n.priority} priority · {title(n.status)} · Follow-up: {n.follow_up_on||'Not scheduled'}{n.follow_up_on&&n.follow_up_on<today&&pending.includes(n.status)?' — overdue':''}</p><button className="secondary" onClick={()=>setChosen(n.id)}>Review need / linked assistance</button></article>)}<Pager page={page} more={more} busy={busy} onChange={setPage}/>{chosen&&<NeedDetail key={chosen} id={chosen} refreshKey={`${rev}-${refreshKey}`} close={()=>setChosen(null)} changed={refresh}/>}
 {personId&&<details className="survey-question"><summary>Record assessed need</summary><p>Choose an approved source survey. Check existing needs first; another request can create a separate need in the same category.</p><form onSubmit={create}><fieldset disabled={busy}><label className="field">Source survey (latest 50 approved)<select name="response" required><option value="">Choose approved response</option>{responses.map(r=><option key={r.id} value={r.id}>{new Date(r.created_at).toLocaleDateString()} · {r.id} · v{r.version}</option>)}</select></label><div className="form-grid"><label className="field">Category<select name="category">{categories.map(c=><option key={c}>{c}</option>)}</select></label><label className="field">Priority<select name="priority" defaultValue="medium">{['low','medium','high'].map(p=><option key={p}>{p}</option>)}</select></label><label className="field">Follow-up date (optional)<input name="followup" type="date"/></label></div><label className="field">Assessed need<textarea name="description" minLength={5} maxLength={1000} required/></label><label className="field">Assessment evidence / reason<textarea name="reason" minLength={5} maxLength={1000} required/></label><p>If a save is uncertain, retry this form unchanged. After closing or refreshing, check the needs list before creating another record.</p><button className="primary">Record need</button></fieldset></form></details>}</section>
}
function NeedDetail({id,close,changed,refreshKey}:{id:string;close:()=>void;changed:()=>void;refreshKey:string}){
 const panel=useRef<HTMLElement>(null);useEffect(()=>{panel.current?.focus()},[id]);
 const [need,setNeed]=useState<Need|null>(null),[entries,setEntries]=useState<Entry[]>([]),[links,setLinks]=useState<Link[]>([]),[linkedEntries,setLinkedEntries]=useState<Record<string,Entry>>({}),[page,setPage]=useState(0),[more,setMore]=useState(false),[linkPage,setLinkPage]=useState(0),[linkMore,setLinkMore]=useState(false),[busy,setBusy]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState(''),[rev,setRev]=useState(0),[history,setHistory]=useState<T['need_revisions']['Row'][]>([]),[linkHistory,setLinkHistory]=useState<T['need_link_revisions']['Row'][]>([]);
 useEffect(()=>{let live=true;setBusy(true);setError('');setHistory([]);setLinkHistory([]);async function load(){const n=await db!.from('beneficiary_needs').select('*').eq('id',id).single();if(n.error)throw n.error;const [e,l]=await Promise.all([db!.from('assistance_entries').select('*').eq('person_id',n.data.person_id).eq('status','recorded').order('delivered_on',{ascending:false}).order('id').range(page*50,page*50+50),db!.from('need_assistance_links').select('*').eq('need_id',id).order('assistance_id').range(linkPage*50,linkPage*50+50)]);if(e.error)throw e.error;if(l.error)throw l.error;const displayed=(l.data||[]).slice(0,50);const a=displayed.length?await db!.from('assistance_entries').select('*').in('id',displayed.map(l=>l.assistance_id)):{data:[],error:null};if(a.error)throw a.error;if(live){setNeed(n.data);setEntries((e.data||[]).slice(0,50));setMore((e.data||[]).length>50);setLinks(displayed);setLinkMore((l.data||[]).length>50);setLinkedEntries(Object.fromEntries((a.data||[]).map(a=>[a.id,a])))}}load().catch(e=>{if(live)setError(e.message)}).finally(()=>{if(live)setBusy(false)});return()=>{live=false}},[id,page,linkPage,rev,refreshKey]);
 async function act(task:()=>Promise<unknown>,msg:string){setBusy(true);setError('');setMessage('');try{await task();setMessage(msg);setRev(n=>n+1);changed()}catch(e){setError((e as Error).message);setBusy(false)}}
 function update(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!need)return;const f=new FormData(e.currentTarget);void act(()=>rpc('update_beneficiary_need',{p_id:id,p_description:val(f,'description'),p_priority:val(f,'priority'),p_status:val(f,'status'),p_follow_up:val(f,'followup')||null,p_reason:val(f,'reason'),p_version:need.version}),'Need reviewed.')}
 function link(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!need)return;const f=new FormData(e.currentTarget),aid=val(f,'assistance');void act(async()=>{const l=await db!.from('need_assistance_links').select('version').eq('need_id',id).eq('assistance_id',aid).maybeSingle();if(l.error)throw l.error;await rpc('set_need_assistance_link',{p_need:id,p_assistance:aid,p_active:true,p_reason:val(f,'reason'),p_need_version:need.version,p_link_version:l.data?.version||0})},'Assistance linked. Confirm the outcome separately.')}
 return <section ref={panel} tabIndex={-1} className="registry-operations" aria-label="Need review"><div className="panel-title"><h3>Review assessed need</h3><button className="secondary" onClick={close}>Close need</button></div>{error&&<p role="alert" className="notice error">{error}</p>}{message&&<p role="status">{message}</p>}{busy&&<p role="status">Loading…</p>}{need&&<><p>{need.category} · {title(need.status)} · v{need.version}</p><p>Last review: {need.last_reason}</p><form key={need.version} onSubmit={update}><fieldset disabled={busy}><label className="field">Description<textarea name="description" required minLength={5} maxLength={1000} defaultValue={need.description}/></label><div className="form-grid"><label className="field">Priority<select name="priority" defaultValue={need.priority}>{['low','medium','high'].map(p=><option key={p}>{p}</option>)}</select></label><label className="field">Status<select name="status" defaultValue={need.status}>{states.map(s=><option key={s} value={s}>{s==='closed'?'closed — not counted as met':title(s)}</option>)}</select></label><label className="field">Follow-up date<input name="followup" type="date" defaultValue={need.follow_up_on||''}/></label></div><label className="field">Outcome / review reason<textarea name="reason" required minLength={5} maxLength={1000}/></label><p>“Met” requires active, recorded assistance links and your outcome assessment. Remove voided links first. “Closed” means no further follow-up, not a successful outcome.</p><button className="primary">Save review</button></fieldset></form>
 <h4>Linked assistance</h4>{!links.length&&!busy&&<p>No links on this page.</p>}{links.map(l=>{const a=linkedEntries[l.assistance_id];return <article className="document-row" key={l.assistance_id}><strong>{a?.description||l.assistance_id}</strong><p>{a?.delivered_on} · {a?.kind==='cash'?`PKR ${a.amount_pkr}`:`${a?.quantity??''} ${a?.unit??''}`} · Delivery {a?.status||'unavailable'} · Link {l.active?'active':'removed'}</p><p>{l.reason}</p>{l.active&&<form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);void act(()=>rpc('set_need_assistance_link',{p_need:id,p_assistance:l.assistance_id,p_active:false,p_reason:val(f,'reason'),p_need_version:need.version,p_link_version:l.version}),'Link removed; history retained.')}}><label className="field">Reason to remove link<input name="reason" required minLength={5} maxLength={1000}/></label><button className="secondary" disabled={busy||need.status==='closed'}>Remove link</button></form>}</article>})}<Pager page={linkPage} more={linkMore} busy={busy} onChange={setLinkPage}/>
 <details className="survey-question"><summary>Link a recorded delivery</summary><p>Choose assistance for this same person and project. Linking does not allocate money or prevent linking a delivery to another need.</p><form onSubmit={link}><fieldset disabled={busy||need.status==='closed'}><label className="field">Recorded assistance<select name="assistance" required><option value="">Choose delivery</option>{entries.map(a=><option key={a.id} value={a.id}>{a.delivered_on} · {a.description} · {a.kind==='cash'?`PKR ${a.amount_pkr}`:`${a.quantity} ${a.unit}`} · {a.id}</option>)}</select></label><label className="field">How this delivery addresses the need<textarea name="reason" required minLength={5} maxLength={1000}/></label><button className="secondary">Link delivery</button></fieldset></form><Pager page={page} more={more} busy={busy} onChange={setPage}/></details>
 <details className="survey-question"><summary>Assessment provenance</summary><p>Source response: {need.source_response_id} · approved version {need.source_response_version}</p><p>Created by {need.created_by} · {new Date(need.created_at).toLocaleString()}</p><p>Last updated by {need.updated_by} · {new Date(need.updated_at).toLocaleString()}</p><pre className="survey-json">{JSON.stringify(need.identity_snapshot,null,2)}</pre></details><button className="secondary" disabled={busy} onClick={async()=>{const [h,l]=await Promise.all([db!.from('need_revisions').select('*').eq('need_id',id).order('version',{ascending:false}).limit(50),db!.from('need_link_revisions').select('*').eq('need_id',id).order('recorded_at',{ascending:false}).order('assistance_id').order('version',{ascending:false}).limit(50)]);if(h.error||l.error)setError(h.error?.message||l.error!.message);else{setHistory(h.data||[]);setLinkHistory(l.data||[])}}}>Show latest 50 assessment and link revisions</button>{history.map(h=><details key={h.version}><summary>Assessment v{h.version} · {new Date(h.recorded_at).toLocaleString()}</summary><pre className="survey-json">{JSON.stringify(h.snapshot,null,2)}</pre></details>)}{linkHistory.map(l=><details key={l.assistance_id+'-'+l.version}><summary>Link {l.assistance_id} · v{l.version}</summary><pre className="survey-json">{JSON.stringify(l.snapshot,null,2)}</pre></details>)}</>}</section>
}
