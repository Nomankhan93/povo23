import {useEffect,useState,type FormEvent} from 'react';
import {db,rpc} from './client';
import type {Database} from './database.types';
import {geographyPath,type Geo} from './Phase12';
type Experience=Database['public']['Tables']['volunteer_experiences']['Row'];
type Opportunity=Database['public']['Tables']['work_opportunities']['Row'];
type Invitation=Database['public']['Tables']['work_invitations']['Row'];
type Org={id:string;name:string;status:string};
const text=(f:FormData,k:string)=>String(f.get(k)||'');
export function ExperiencePanel({userId,organization,orgs,readonly=false}:{userId:string;organization:string|null;orgs:Org[];readonly?:boolean}){
 const [rows,setRows]=useState<Experience[]>([]),[editing,setEditing]=useState<Experience|null>(null),[create,setCreate]=useState(false),[page,setPage]=useState(0),[more,setMore]=useState(false),[revision,setRevision]=useState(0),[busy,setBusy]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');
 useEffect(()=>{let live=true;setBusy(true);setRows([]);setError('');let q=db!.from('volunteer_experiences').select('*').order('created_at',{ascending:false}).order('id').range(page*50,page*50+50);q=organization?q.eq('organization_id',organization).neq('status','unverified'):q.eq('user_id',userId);if(readonly)q=q.eq('status','verified');q.then(r=>{if(!live)return;if(r.error)setError(r.error.message);else{setRows((r.data||[]).slice(0,50));setMore((r.data||[]).length>50)}setBusy(false)});return()=>{live=false}},[userId,organization,readonly,page,revision]);
 async function act(fn:()=>Promise<unknown>,success:string){setBusy(true);setError('');setMessage('');try{await fn();setEditing(null);setCreate(false);setMessage(success);setRevision(n=>n+1)}catch(e){setError((e as Error).message);setBusy(false)}}
 function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);act(()=>rpc('save_experience',{p_id:editing?.id||null,p_org:editing?.organization_id||text(f,'org'),p_role:text(f,'role'),p_start:text(f,'start'),p_end:text(f,'end')||null,p_description:text(f,'description'),p_request:f.get('request')==='on',p_version:editing?.version||0}),'Experience saved. Any previous verification has been reset.')}
 return <section className="panel detail"><div className="panel-title"><h2>{readonly?'NGO-confirmed experience':organization?'Experience confirmation requests':'My work experience'}</h2>{!readonly&&!organization&&<button className="primary" onClick={()=>{setEditing(null);setCreate(true)}}>Add experience</button>}</div><p>{organization?'Confirm only work your NGO can substantiate. This does not verify the complete volunteer profile.':readonly?'These entries were confirmed by their respective NGOs.':'Requesting confirmation shares this entry and your name with the selected NGO. It does not grant access to your CV or private documents.'}</p>{error&&<p className="notice error" role="alert">{error}</p>}{message&&<p role="status">{message}</p>}{(create||editing)&&<form key={editing?.id||'new'} onSubmit={save}><div className="form-grid"><label className="field">NGO<select name="org" required disabled={!!editing} defaultValue={editing?.organization_id||''}><option value="">Choose NGO</option>{orgs.filter(o=>o.status==='active'||o.id===editing?.organization_id).map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select></label><label className="field">Role<input name="role" minLength={2} maxLength={120} required defaultValue={editing?.role_title}/></label><label className="field">Start date<input name="start" type="date" required defaultValue={editing?.start_date}/></label><label className="field">End date (blank if ongoing)<input name="end" type="date" defaultValue={editing?.end_date||''}/></label></div><label className="field">Work performed<textarea name="description" minLength={10} maxLength={2000} required defaultValue={editing?.description}/></label><label className="checklabel"><input type="checkbox" name="request"/>Request NGO confirmation and share this entry with its admins</label><div className="actions"><button type="button" className="secondary" onClick={()=>{setEditing(null);setCreate(false)}}>Cancel</button><button className="primary" disabled={busy}>Save experience</button></div></form>}{busy&&<p role="status">Loading…</p>}{rows.map(e=><article className="document-row" key={e.id}><div className="document-heading"><h3>{e.role_title}</h3><span className={'badge '+(e.status==='verified'?'verified':'pending')}>{e.status==='verified'?'NGO-confirmed':e.status}</span></div><p>{organization?e.volunteer_name:orgs.find(o=>o.id===e.organization_id)?.name||`NGO ${e.organization_id}`} · {e.start_date} – {e.end_date||'Present'}</p><p className="preserve-lines">{e.description}</p>{e.reviewed_at&&<p>Confirmed/reviewed by NGO {orgs.find(o=>o.id===e.organization_id)?.name||e.organization_id} · {new Date(e.reviewed_at).toLocaleDateString()}</p>}{!readonly&&e.review_note&&<p>Review: {e.review_note}</p>}{!readonly&&!organization&&<button className="secondary" onClick={()=>{setEditing(e);setCreate(false)}}>Edit / request again</button>}{!readonly&&organization&&e.status==='pending'&&e.user_id!==userId&&<form className="review" onSubmit={event=>{event.preventDefault();const f=new FormData(event.currentTarget);act(()=>rpc('review_experience',{p_id:e.id,p_status:text(f,'status'),p_note:text(f,'note'),p_version:e.version}),'Experience decision saved.')}}><label className="field">Decision<select name="status"><option value="verified">Confirm experience</option><option value="rejected">Reject claim</option></select></label><label className="field">Reason / checks performed<textarea name="note" required minLength={3} maxLength={2000}/></label><button className="primary" disabled={busy}>Save decision</button></form>}</article>)}{!busy&&!rows.length&&<p>No experience entries on this page.</p>}<div className="actions"><button className="secondary" disabled={busy||!page} onClick={()=>setPage(n=>n-1)}>Previous</button><button className="secondary" disabled={busy||!more} onClick={()=>setPage(n=>n+1)}>Next 50</button><button className="secondary" disabled={busy} onClick={()=>setRevision(n=>n+1)}>Refresh</button></div></section>
}
export function InvitationsPanel({userId,organization,orgs,geographies}:{userId:string;organization:string|null;orgs:Org[];geographies:Geo[]}){
 const [opportunities,setOpportunities]=useState<Opportunity[]>([]),[invitations,setInvitations]=useState<Invitation[]>([]),[create,setCreate]=useState(false),[busy,setBusy]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState(''),[revision,setRevision]=useState(0),[page,setPage]=useState(0),[opPage,setOpPage]=useState(0),[more,setMore]=useState(false),[opMore,setOpMore]=useState(false),[clock,setClock]=useState(Date.now());
 useEffect(()=>{const id=setInterval(()=>setClock(Date.now()),30000);return()=>clearInterval(id)},[]);
 useEffect(()=>{let live=true;setBusy(true);setError('');setInvitations([]);setOpportunities([]);async function load(){let q=db!.from('work_invitations').select('*').order('created_at',{ascending:false}).order('id').range(page*50,page*50+50);q=organization?q.eq('organization_id',organization):q.eq('user_id',userId);const i=await q;if(i.error)throw i.error;const list=(i.data||[]).slice(0,50);let ops:Opportunity[]=[];let om=false;if(organization){const o=await db!.from('work_opportunities').select('*').eq('organization_id',organization).order('created_at',{ascending:false}).order('id').range(opPage*50,opPage*50+50);if(o.error)throw o.error;ops=(o.data||[]).slice(0,50);om=(o.data||[]).length>50;}const missing=[...new Set(list.map(i=>i.opportunity_id))].filter(id=>!ops.some(o=>o.id===id));let linked:Opportunity[]=[];if(missing.length){const r=await db!.from('work_opportunities').select('*').in('id',missing);if(r.error)throw r.error;linked=r.data||[];}if(live){setInvitations(list);setMore((i.data||[]).length>50);setOpportunities([...ops,...linked]);setOpMore(om);setPrimaryIds(ops.map(o=>o.id))}}load().catch(e=>{if(live)setError(e.message)}).finally(()=>{if(live)setBusy(false)});return()=>{live=false}},[organization,userId,revision,page,opPage]);
 const [primaryIds,setPrimaryIds]=useState<string[]>([]);
 async function act(fn:()=>Promise<unknown>,success:string){setBusy(true);setError('');setMessage('');try{await fn();setCreate(false);setMessage(success);setRevision(n=>n+1)}catch(e){setError((e as Error).message);setBusy(false)}}
 function newOpportunity(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);act(()=>rpc('create_opportunity',{p_org:organization,p_title:text(f,'title'),p_description:text(f,'description'),p_geography:text(f,'geo'),p_start:text(f,'start'),p_end:text(f,'end'),p_reply_by:new Date(text(f,'reply')).toISOString(),p_payment:text(f,'payment'),p_payment_note:text(f,'note')}),'Opportunity created. Its details are fixed; close it and create another if terms change.')}
 return <section className="panel detail"><div className="panel-title"><h2>{organization?'NGO opportunities & invitations':'My invitations'}</h2>{organization&&<button className="primary" onClick={()=>setCreate(!create)}>Create opportunity</button>}</div><p>Accepting indicates interest in this opportunity. It does not create a paid contract, payment approval, assignment or additional document access.</p>{error&&<p className="notice error" role="alert">{error}</p>}{message&&<p role="status">{message}</p>}{create&&<form onSubmit={newOpportunity}><div className="form-grid"><label className="field">Title<input name="title" required minLength={3} maxLength={150}/></label><label className="field">Work area<select name="geo" required><option value="">Select area</option>{geographies.filter(g=>geographyPath(g.id,geographies).every(n=>n.active)).map(g=><option key={g.id} value={g.id}>{geographyPath(g.id,geographies).map(n=>n.name).join(' / ')}</option>)}</select></label><label className="field">Start date<input type="date" name="start" required/></label><label className="field">End date<input type="date" name="end" required/></label><label className="field">Reply deadline (your local time)<input type="datetime-local" name="reply" required/></label><label className="field">Payment preference<select name="payment"><option value="unpaid">Unpaid</option><option value="paid">Paid</option></select></label></div><label className="field">Expected tasks<textarea name="description" minLength={10} maxLength={4000} required/></label><label className="field">Proposed payment / expense details<textarea name="note" maxLength={1000}/></label><button className="primary" disabled={busy}>Create opportunity</button></form>}{busy&&<p role="status">Loading…</p>}{organization&&<><h3>Your opportunities</h3>{opportunities.filter(o=>primaryIds.includes(o.id)).map(o=><article className="document-row" key={o.id}><h3>{o.title}</h3><OpportunityDetails o={o} geographies={geographies}/><p>Status: {o.status==='open'&&Date.parse(o.reply_by)<=clock?'Reply deadline passed':o.status}</p>{o.status==='open'&&<button className="secondary" disabled={busy} onClick={()=>{if(window.confirm('Close this opportunity and cancel all pending invitations? Accepted responses remain in history.'))act(()=>rpc('close_opportunity',{p_id:o.id}),'Opportunity closed.')}}>Close opportunity</button>}</article>)}<div className="actions"><button className="secondary" disabled={busy||!opPage} onClick={()=>setOpPage(n=>n-1)}>Previous opportunities</button><button className="secondary" disabled={busy||!opMore} onClick={()=>setOpPage(n=>n+1)}>Next 50 opportunities</button></div><p>To invite someone, open Volunteers, shortlist them, then use Invite to opportunity on their directory row.</p></>}<h3>Invitation history</h3>{invitations.map(i=>{const o=opportunities.find(o=>o.id===i.opportunity_id);const expired=!!o&&Date.parse(o.reply_by)<=clock;const active=i.status==='pending'&&o?.status==='open'&&!expired;return <article className="document-row" key={i.id}><h3>{o?.title||'Opportunity unavailable'}</h3><p>{organization?i.volunteer_name:orgs.find(n=>n.id===i.organization_id)?.name||'Partner NGO'}</p><span className="badge">{i.status==='pending'&&expired?'expired':i.status}</span>{o&&<OpportunityDetails o={o} geographies={geographies}/>}<div className="actions">{active&&!organization&&<><button className="primary" disabled={busy} onClick={()=>act(()=>rpc('respond_work_invitation',{p_id:i.id,p_status:'accepted',p_version:i.version}),'Invitation accepted. NGO notified.')}>Accept invitation</button><button className="secondary" disabled={busy} onClick={()=>act(()=>rpc('respond_work_invitation',{p_id:i.id,p_status:'declined',p_version:i.version}),'Invitation declined.')}>Decline</button></>}{organization&&i.status==='pending'&&<button className="secondary" disabled={busy} onClick={()=>act(()=>rpc('cancel_work_invitation',{p_id:i.id,p_version:i.version}),'Invitation cancelled.')}>Cancel invitation</button>}</div></article>})}{!busy&&!invitations.length&&<p>No invitations on this page.</p>}<div className="actions"><button className="secondary" disabled={busy||!page} onClick={()=>setPage(n=>n-1)}>Previous invitations</button><button className="secondary" disabled={busy||!more} onClick={()=>setPage(n=>n+1)}>Next 50 invitations</button><button className="secondary" disabled={busy} onClick={()=>setRevision(n=>n+1)}>Refresh</button></div></section>
}
function OpportunityDetails({o,geographies}:{o:Opportunity;geographies:Geo[]}){return <><p>{geographyPath(o.geography_id,geographies).map(n=>n.name).join(' / ')} · {o.start_date} – {o.end_date}</p><p className="preserve-lines">{o.description}</p><p>{o.payment_type} · {o.payment_note||'No additional terms'}</p><p>Reply by {new Date(o.reply_by).toLocaleString()}</p></>}
export function InviteVolunteer({organization,userId,name}:{organization:string;userId:string;name:string}){
 const [open,setOpen]=useState(false),[rows,setRows]=useState<Opportunity[]>([]),[page,setPage]=useState(0),[more,setMore]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
 useEffect(()=>{if(!open)return;let live=true;setBusy(true);setRows([]);db!.from('work_opportunities').select('*').eq('organization_id',organization).eq('status','open').gt('reply_by',new Date().toISOString()).order('created_at',{ascending:false}).order('id').range(page*50,page*50+50).then(r=>{if(!live)return;if(r.error)setError(r.error.message);else{setRows((r.data||[]).slice(0,50));setMore((r.data||[]).length>50)}setBusy(false)});return()=>{live=false}},[open,organization,page]);
 return <div><button className="link" onClick={()=>setOpen(!open)}>Invite to opportunity</button>{open&&<div className="invite-inline">{error&&<p role="alert">{error}</p>}{message&&<p role="status">{message}</p>}<form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);setError('');setMessage('');try{await rpc('send_work_invitation',{p_opportunity:text(f,'op'),p_user:userId});setMessage('Invitation sent.')}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><label className="field">Invite {name}<select name="op" required><option value="">Choose opportunity</option>{rows.map(o=><option key={o.id} value={o.id}>{o.title} · {o.start_date}</option>)}</select></label><button className="secondary" disabled={busy||!rows.length}>Send invitation</button></form>{!busy&&!rows.length&&<p>Create an open opportunity in Invitations first.</p>}<div className="actions"><button className="secondary" disabled={busy||!page} onClick={()=>setPage(n=>n-1)}>Previous</button><button className="secondary" disabled={busy||!more} onClick={()=>setPage(n=>n+1)}>Next</button></div></div>}</div>
}
